# Issues
- npm install failing on Windows due to missing Visual Studio build tools for native modules (better-sqlite3).
- Node version (v24) may not have prebuilt binaries for some native deps; consider downgrading to Node 18/20 for compatibility.
